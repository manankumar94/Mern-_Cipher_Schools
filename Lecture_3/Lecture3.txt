git add . (also add Modified changes)

//checkout
git checkout TRACK_ID (from git log --online) - tell use how project was at that commit now <git log> it show only upto that particular commit only.

git checkout master - to go back present thing

NOTE: If you want to change history you can but not in same BRANCH_NAME